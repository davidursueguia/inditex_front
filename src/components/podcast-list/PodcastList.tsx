const PodcastList = () => {
    return (
        <div style={{height: '100vh', width: '100%', display: 'flex', justifyContent: 'center', alignItems: 'center'}}>
            <span>This is the podcast list</span>
        </div>
    );
};

export default PodcastList;
